[project]
name = "mock-provider"
version = "0.1.0"
description = "Mock provider for Payflow"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[project.optional-dependencies]
dev = [
    "httpx",
    "pytest",
    "pytest-asyncio",
]
